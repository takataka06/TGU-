<div class="container">
  <h1 class="my-4">ユーザー情報</h1>

  <div class="card mb-4 shadow-sm">
    <div class="card-body">
      <h5 class="card-title">ユーザー名: <%= @user.name.present? ? @user.name : "名無し" %></h5>
      <p class="card-text">登録日: <%= @user.created_at.strftime("%Y/%m/%d") %></p>
    </div>
  </div>

  <h2 class="my-4">投稿一覧</h2>
  <% if @posts.any? %>
    <div class="list-group">
      <% @posts.each do |post| %>
        <a href="<%= post_path(post) %>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
          <span><%= post.title %></span>
          <small class="text-muted"><%= post.created_at.strftime("%Y/%m/%d %H:%M") %></small>
        </a>
      <% end %>
    </div>
  <% else %>
    <p>このユーザーはまだ投稿をしていません。</p>
  <% end %> 
</div>